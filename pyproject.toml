[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "gridconnect"
version = "0.1.0"
dependencies = [
    "requests",
    "python-dotenv",
    "psycopg2-binary",
    "playwright"
]

[tool.setuptools.packages.find]
where = ["src"]